# 词根背单词 · 手机安装说明

这是一款 **PWA（渐进式 Web 应用）**：本质是经过"可安装化"改造的网页应用，安装后像原生 App 一样拥有桌面图标、全屏独立窗口、离线可用，**安卓和苹果都支持**，且无需应用商店审核。

> 为什么不是 .apk / .ipa 文件？
> - 安卓 .apk 需要在电脑上安装 Android Studio + SDK 才能编译签名；
> - 苹果 .ipa 必须在 Mac 上用 Xcode 编译，且个人设备安装需要 99 美元/年的开发者证书。
> - PWA 是目前零成本、当天就能装到手机上的最优方案，体验与原生 App 基本一致。后续若要上架应用商店，可直接用本包作为 Capacitor / WebView 壳的网页资源。

---

## 第一步：把应用部署到一个 https 网址（5 分钟）

安装到手机的前提是应用有一个 **https 开头的访问地址**。三选一：

### 方案 A：GitHub Pages（免费，推荐）
1. 注册/登录 github.com，新建一个公开仓库，如 `word-game`
2. 把本压缩包里的所有文件上传到仓库（网页端 "upload files" 即可）
3. 仓库 Settings → Pages → Source 选 main 分支 → Save
4. 等待 1-2 分钟，得到地址：`https://你的用户名.github.io/word-game/`

### 方案 B：Vercel / Netlify（免费，更快）
1. 登录 vercel.com 或 netlify.com
2. 直接把本压缩包拖进去（drag & drop），秒得一个 https 地址

### 方案 C：自己的服务器 / 群晖等
把文件放到任意支持 https 的静态目录即可。

---

## 第二步：安卓手机安装

1. 用手机 Chrome 打开上面得到的 https 地址
2. 点右上角 **⋮ 菜单** → **"安装应用"**（或"添加到主屏幕"）
3. 确认安装，桌面出现「词根背单词」图标，点开即用
4. 首次加载后**离线也能用**（所有单词数据已缓存在手机本地）

> 华为等无 Chrome 的机型：用系统浏览器打开地址，菜单里找"添加到桌面"。

## 第二步：苹果手机安装

1. 用 **Safari** 打开 https 地址（注意：必须是 Safari，微信里要点"在 Safari 打开"）
2. 点底部分享按钮 <img> → 下滑找到 **"添加到主屏幕"** → 添加
3. 桌面出现图标，点开即全屏运行

---

## 临时试用（不安装，仅同一 WiFi 下）

电脑本机预览不受影响：在本目录运行 `npm run dev` 后用电脑浏览器打开即可。
手机临时体验：电脑运行 `npm run dev`，手机连**同一 WiFi**，浏览器访问 `http://电脑的局域网IP:7100`（如 http://192.168.1.5:7100，IP 可用 `ipconfig` 查看）。
注意：局域网 http 地址只能浏览器里用，不能"安装到桌面"。

---

## 常见问题

- **两台手机数据同步吗？** 不同步。账号与学习数据保存在各自手机浏览器本地，换手机需重新注册。
- **换手机/重装后记录会丢吗？** 数据在该手机的浏览器存储里，不清除浏览器数据就不会丢。
- **听音写词没声音？** 检查手机静音开关（iPhone 的静音拨片会影响网页语音），并确认系统语言环境下 TTS 可用。
- **苹果添加到主屏幕后打开是白屏？** 确认是用 Safari 添加的，且地址是 https。

## 文件清单

| 文件 | 作用 |
|---|---|
| index.html / app.js / srs.js / words_data.js | 应用本体（651 词库 + 游戏逻辑 + 记忆曲线调度） |
| sw.js | 离线缓存（Service Worker） |
| manifest.webmanifest | 安装到桌面的清单（名称/图标/全屏模式） |
| icon-192.png / icon-512.png / apple-touch-icon.png | 桌面图标 |
| server.js / package.json | 电脑本机预览用（`npm run dev`） |
